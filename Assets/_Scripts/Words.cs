using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Words : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log(wordbank.getWord());
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
